[workspace]
# Exclude proc-macro crates that have no tests
exclude = [
    "catga-core-macros",
]

# Exclude catga-tests package - it has broken test files with unresolved imports
do_not_report = [
    "catga-tests",
]
