
## 固定使用 TDD 开发模式

所有功能开发和缺陷修复都必须遵循 Red-Green-Refactor 三段式：

1. **Red（红）**：先在对应 crate 的 `tests/` 目录添加或修改测试，运行最小定向测试，并确认测试因为待实现行为而失败，而不是因为环境、语法或测试本身错误。
2. **Green（绿）**：只实现让测试通过所需的最小生产代码，持续运行定向测试，直到新增测试和相关回归测试全部通过。
3. **Refactor（重构）**：在测试保护下消除重复、改善命名和边界、完善错误处理与文档；随后运行定向测试、业务 E2E 和提交前全量门禁。

不得先完成生产实现再补形式化测试。修复缺陷时，必须先添加能够稳定复现缺陷的回归测试。测试应验证外部契约和可观察业务结果，不应依赖 CQRS、队列、缓存等内部实现细节代替业务验证。

## 设计准则

所有设计和审查必须同时考虑以下四项：

- **简单**：优先最少概念、最小状态空间和清晰数据流；不要为假设中的未来需求提前抽象。
- **性能**：避免不必要的分配、复制、锁竞争、无界队列和阻塞异步运行时；优化必须基于明确热路径或测量结果。
- **文档**：让模块职责、公共契约、失败语义、持久化语义和关键权衡可被维护者直接理解。
- **可扩展**：通过稳定边界、组合和明确 trait 扩展，而不是复制流程或扩散特殊分支；扩展性不能以明显增加当前复杂度为代价。

## 工程实现补充原则

1. **DRY 与文件粒度**：DRY 原则优先，重复逻辑应收敛到职责明确的复用边界；同时避免职责过多的大文件和只有零碎转发的过多小文件。拆分以高内聚、低耦合和可独立测试为依据，不按行数机械拆分。
2. **简单的对外 API 与可测试文档**：对外暴露保持最小、稳定和易用，复杂度封装在内部；公共 Rustdoc 示例应能由 doctest 编译或执行。仅在示例确实依赖外部服务时使用 `no_run`，并保证示例仍可编译。
3. **惯用 Rust 风格**：代码结构、命名、所有权、错误处理、迭代器和并发模型必须符合 Rust 最佳实践，优先使用标准、清晰且可被 Clippy 验证的惯用写法。
4. **函数内注释最小化**：函数体内只在关键算法、不直观不变量、安全边界或重要权衡处写注释；注释解释“为什么”，不复述代码“做什么”。能通过命名和结构表达的内容不写注释。
5. **修改与重构同样遵循 TDD**：修改和重构也必须走 Red-Green-Refactor。可以暂时删除或改写与旧实现强耦合、已经失效的具体断言或测试用例，但不得删除测试文件或移除外部契约覆盖；最迟在 Green 完成前写回等价或更强的测试，并先确认新测试因目标行为尚未实现而进入 Red。
6. **回归与审计范围**：所有代码改动都必须运行相关回归测试。涉及架构、公共 API、持久化、并发模型、安全边界或关键性能路径的改动，必须同时执行影响审计并记录风险、兼容性和验证证据；其他改动审计可按风险省略，但引入或升级第三方依赖仍必须执行本文件规定的依赖前置审计。
7. **Tokio 多线程策略**：服务运行时优先使用 Tokio 多线程 runtime，但只在存在真实并发工作负载时使用并发任务；禁止为简单顺序工作无意义地 `spawn`。CPU 密集或阻塞工作应使用有界的专用机制（例如 `spawn_blocking`），并评估线程、任务和切换开销。
8. **最小化引用**：crate、feature 和 `use` 引用均遵循最小化原则。仅启用实际使用的 feature，优先导入具体类型或 trait，避免全量 feature、无必要的 glob 导入和仅为便利引入整套依赖。
9. **薄入口与友好模块划分**：`main.rs` 只负责配置加载、依赖装配、启动和退出；可测试逻辑必须下沉。`lib.rs` 只负责 crate 文档、最小公开面和模块组织，也不得堆积业务实现。生产代码按职责放入命名清晰的目录和文件，文件名应直接表达业务能力。
10. **所有权与分配最小化**：能借用就借用，减少不必要的 `clone`、复制和中间分配；除非无法用安全 Rust 实现且有完整安全论证，否则不得使用 `unsafe`。创建 `Vec` 等集合时，如果容量可可靠预估，应使用 `with_capacity` 或等价方式预分配，但不得为不确定上限过度预留。
11. **统一 crate 布局**：每个 crate 使用 `src/` 存放生产代码、`tests/` 存放测试代码；需要 benchmark 时使用 Rust 约定的 `benches/`，`benches/` 不是强制目录。不得创建空目录或把测试、benchmark 混入 `src/`。
12. **依赖版本选择**：新增或升级 Rust crate 时优先选择最新稳定且与当前 MSRV 和生态兼容的版本，然后在 workspace 中精确约束并提交锁文件。只有确认存在缺陷、兼容性问题、MSRV 限制或用户明确指定时才使用较旧版本，并在依赖审计记录中说明原因和升级条件。

优先复用成熟库，避免自研协议层。引入或升级第三方库前必须前置审计，至少检查：

- 项目活跃度、维护状态、发布历史和 API 稳定性；
- Rust 1.96/MSRV、edition 和现有异步生态兼容性；
- 安全公告、许可证及供应链风险；
- 默认 feature、传递依赖、编译体积和运行时开销；
- 是否与 workspace 现有依赖重复，能否通过关闭默认 feature 缩小依赖面；
- 是否存在官方或更成熟的实现，以及迁移和退出成本。

依赖应在 workspace 统一管理并尽可能精确锁定。提交前使用 `cargo tree` 审查直接依赖和重要传递依赖，不得引入来源可疑或名称近似的包。

## Rust 最佳实践

Rust 最佳实践是强制设计规范，除非确有特殊情况，不允许突破：

- 使用类型系统表达不变量，优先所有权、借用、枚举和 newtype，而不是字符串约定或隐式状态。
- 生产代码使用结构化错误和明确的 `Result` 传播；不得用 `unwrap`、`expect` 或 panic 处理可恢复错误。
- 异步代码不得执行阻塞 I/O；并发、队列、重试和超时必须有界，并明确取消与资源释放语义。
- 保持 API 最小化，默认私有；仅在存在真实跨模块契约时公开类型或函数。
- 避免不必要的 clone、动态分派和共享可变状态；性能优化不得牺牲正确性与可读性。
- `unsafe`、绕过类型约束或偏离惯用 Rust 的实现必须有无法使用安全 Rust 的明确理由、完整安全说明和专项测试。

特殊情况必须在代码评审说明中记录原因、风险、替代方案和验证证据，不得仅以“方便”或“暂时”为理由突破规范。

## `allow`、死代码与警告

默认不新增任何 `#[allow(...)]` 或 crate 级 `#![allow(...)]`。确实无法消除的外部兼容性问题，必须将 `allow` 限定在最小作用域，并写明原因和移除条件。

不得使用 `allow(dead_code)`、下划线重命名或虚假调用掩盖无用代码。无生产用途的函数、类型、feature、依赖和兼容层应直接删除。所有警告都应修复，Clippy 以 `-D warnings` 运行。

## Rustdoc 要求

Rustdoc 必须完整。开发过程中可以暂缓撰写，但最迟必须在提交前补齐并复核：

- crate 和重要模块使用 `//!` 说明职责与边界；
- 所有新增或修改的公共 API 使用 `///` 描述用途、参数语义、返回值和关键不变量；
- 可能失败的 API 提供 `# Errors`，可能 panic 的 API 提供 `# Panics`，`unsafe` API 必须提供 `# Safety`；
- 对不直观的公共 API 提供可编译示例；
- 文档必须描述实际行为，不得保留过期实现细节、编号引用或无效链接。

提交前必须人工检查公共 API 文档完整性，并执行：

```bash
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
```

## 目录与测试代码规范

目录必须按职责组织：

- 业务和基础设施生产代码放在各 crate 的 `src/` 中；
- 测试代码只能放在对应 crate 的 `tests/` 目录中；
- `src/` 中禁止 `#[cfg(test)] mod tests`、测试 fixture、mock、fake 或仅供测试调用的辅助代码；
- 多个集成测试共享的 fixture 放在 `tests/common/`，不得为了测试便利扩大生产 API；
- 可选 benchmark 放在 `benches/`，不得混入业务模块；
- 一个模块只承担清晰、单一的职责，跨 crate 共享契约放在合适的 shared crate，具体适配器放在 adapters crate。

测试文件应按业务能力命名，并优先使用公开入口。单元/组件测试不能替代真实 HTTP、MCP、事件源、持久化和重启语义的业务 E2E。

## 性能测试

性能测试不是每次改动的强制门禁，但 Agent 应主动识别并提醒用户关注关键热路径，例如：

- 文档切分、embedding 批处理和向量写入；
- 高并发 HTTP/SSE 或 MCP 请求；
- 队列、去重、缓存、持久化和恢复；
- 大规模 reconciliation、序列化和锁竞争。

当改动影响关键热路径、复杂度或内存分配时，建议在 `benches/` 添加可重复 benchmark，并记录基线、数据规模和比较结果。没有性能证据时，不得声称性能得到提升。

## 提交前质量门禁

提交前必须满足以下全部条件，不接受“基本通过”或忽略失败：

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo build --workspace
cargo test --workspace
./scripts/test-e2e.sh
RUSTDOCFLAGS="-D warnings -W missing-docs" cargo doc --workspace --no-deps
./scripts/audit-dependencies.sh
```

门禁标准：

- 格式检查和严格 Clippy 100% 通过，无 warning；
- workspace 编译 100% 成功；
- 全部测试和 doctest 100% 通过，无跳过失败；
- 独立业务 E2E 100% 通过；
- Rustdoc 构建无 warning，公共 API 文档完整；
- 无无用代码、无无理由的 `allow`、无测试代码混入 `src/`。

任何一项失败都必须先修复再提交，不得通过跳过测试、降低 lint 等级或增加宽泛 `allow` 绕过门禁。
